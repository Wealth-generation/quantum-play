"use client";

// The idle ball is now driven by the persistent Pixi ball in RoulettePixiBallStage.
// This CSS raster component is retired so only one ball exists across idle + spin.
export function RouletteWheelBall() {
  return null;
}
