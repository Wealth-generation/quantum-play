# Hook Policy

No active hooks are installed in this baseline.

Hooks are policy/templates only for now. No hook may auto-edit, auto-stage, auto-commit, auto-push, auto-merge, delete branches, or archive tasks.

Future hooks require explicit approval, documented purpose, scope, rollback plan, and validation evidence.
