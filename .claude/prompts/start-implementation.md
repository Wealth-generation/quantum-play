# Start Implementation

Read `CLAUDE.md`, `docs/architecture/foundation-decisions.md`, `.claude/rules/index.md`, relevant rule files, `.claude/skills/implementation/SKILL.md`, and the active task artifact if applicable.

Implement only explicit approved scope. Keep editable files separate from context-only files. Do not stage, commit, push, create PRs, or create forbidden files/folders.
