# Task Lifecycle Record

## Identity

- Task title: Plinko Animation Redesign
- Status: active; integrated production renderer rebuild in progress; manual UI QA pending
- Mode: implementation; Phase 0, Phase 1, Phase 2, Phase 3, Phase 4A, Phase 4B, Phase 4C, and Phase 4D implementation complete; Phase 4D visual-remediation implementation complete; manual UI QA rerun required
- Branch mode: PR-mode
- Base branch: develop
- Task branch: codex/plinko-animation-redesign
- Current branch at task start: develop
- Branch creation command/evidence:
  - `git status --short --branch` -> `## develop...origin/develop`
  - `git switch -c codex/plinko-animation-redesign` failed in the sandbox because Git could not create the ref directory.
  - The same command reran with user-approved escalation -> switched to `codex/plinko-animation-redesign`.

## Scope

- Goal: Progress the approved Plinko renderer-local bucket-target animation redesign while preserving accepted-round settlement authority.
- Completed Phase 2: Added unused renderer-local target-only playback engine and semantic normal/Turbo profile boundaries.
- Completed Phase 3: Added unused renderer-local seeded corridor and target-only fallback planning foundations.
- Completed Phase 4A: Added an unused target-only Matter trajectory builder with soft corridor/funnel guidance and target-only invalid fallback signaling.
- Completed Phase 4B: Wired the target-only Matter trajectory into Pixi through a narrow resolver; valid target-reaching trajectories now animate first, while the legacy raw-path Matter/motion-plan flow remains an explicit transition fallback.
- Phase 4B lifecycle follow-up: pending trajectory resolution now retains its accepted round and token so resize/board-change cancellation settles pending rounds immediately as `cancelled` while destroy continues to invalidate without settlement.
- Completed Phase 4C implementation: added typed target trajectory status/source reporting, development-only source diagnostics, and conservative target-only corridor/guidance widening after failed manual visual QA.
- Completed Phase 4D implementation: replaced the resolver-primary corridor/target trajectory with a bounded renderer-local Matter bounce-core candidate simulation that records physical peg contacts and drives Pixi peg response. Legacy path playback remains temporary development safety only.
- Phase 4D visual QA failed: `.ai/context/plinko-mvp/cur-bounce-4d-replaced.gif` still read as guided/step-like motion with weak peg response, so Phase 4D was visually blocked pending the approved bounce-core correction.
- Completed Phase 4D visual-remediation implementation: candidate-only simulations are now ranked for renderer-local physical quality; contact measurement records collision kinematics; replay preserves physical sample timing; and impact-scaled peg response uses the normal/Turbo playback profile. Manual QA must be rerun before Phase 4D can be approved.
- Approved Phase 0:
  - Confirm PR-mode branch state and create/switch the approved task branch after user confirmation.
  - Create this required active task artifact.
- Approved Phase 1:
  - Add renderer-local `PlinkoVisualTarget` types and `createPlinkoVisualTarget()`.
  - Derive `targetBucketIndex` exactly once from the accepted result's `results` array.
  - Include rows, risk, round identity, bet identity, and a deterministic visual seed.
  - Keep the adapter unused by runtime playback.
- Approved Phase 2:
  - Add renderer-local target-only playback engine and semantic normal/Turbo profile boundaries.
  - Keep the engine unused so current Pixi/Matter playback, renderer settlement callbacks, and the Pixi rendering interface remain unchanged.
  - Do not delegate to legacy path-driven playback through the new engine boundary.
- Approved Phase 3:
  - Add an unused renderer-local target-only corridor plan with varied launch, broad vertical bands, and lower-board funnel metadata.
  - Add a target-only fallback plan that remains separate from the existing path-driven fallback.
  - Keep current Pixi/Matter playback unchanged and do not pass raw `results` into the new planner or engine.
- Approved Phase 4A:
  - Add an unused renderer-local Matter trajectory builder consuming only target, geometry, profile, and corridor plan.
  - Return the existing sampled-trajectory result shape when Matter reaches the target bucket; otherwise return a target-only invalid result for future fallback handling.
  - Keep existing path-driven Matter/Pixi runtime playback unchanged.
- Approved Phase 4B:
  - Derive the renderer-local visual target from each accepted round, select the existing normal/Turbo signal, and resolve a target-only Matter trajectory through a renderer-local seam.
  - Animate the target-only trajectory only when it is complete and reaches the accepted target bucket.
  - Keep the existing raw-path Matter trajectory and motion-plan behavior as an explicit Phase 4B/5 migration fallback when target-only planning is invalid, incomplete, mismatched, or throws.
  - Preserve the existing Pixi settlement callback, round identity, cancellation behavior, and accepted backend result as business truth.
- Approved Phase 4C:
  - Diagnose the failed manual visual QA and make the selected target-only or legacy fallback trajectory source inspectable without changing business settlement authority.
  - Widen target-only launch/corridor freedom, defer target alignment, and reduce upper/middle steering while retaining lower-board funneling and the existing safety fallback.
  - Preserve Phase 4B pending/active cancellation behavior, legacy fallback, raw-result boundaries, and renderer-local ownership.
- Approved Phase 4D:
  - Replace the primary visible animation model with candidate-only gravity and real peg-collision playback consuming only geometry, `PlinkoVisualTarget`, and profile.
  - Select only a naturally target-reaching candidate, expose `bounce-core` source diagnostics, and retain legacy Matter/path and motion-plan playback only as non-passing development safety.
  - Produce renderer-local physical contact data, bounded contact-driven micro-stall timing, and visible per-peg Pixi response without changing business settlement authority.
- Non-goals:
  - BFF/backend response changes or public result-contract changes.
  - Payout, balance, mini-history, AutoBet, Infinity request pacing, Turbo product semantics, or Provably Fair behavior changes.
  - Dependencies, shared game shell, page layout outside the renderer, test infrastructure, staging, commits, pushes, PRs, merges, branch deletion, or archival.
- Editable files:
  - `.ai/tasks/active/plinko-animation-redesign.md`
  - `src/games/plinko/renderer/plinko-renderer-types.ts`
  - `src/games/plinko/renderer/plinko-visual-target.ts`
  - `src/games/plinko/renderer/plinko-playback-profile.ts`
  - `src/games/plinko/renderer/plinko-playback-engine.ts`
  - `src/games/plinko/renderer/plinko-corridor-plan.ts`
  - `src/games/plinko/renderer/matter-plinko-target-trajectory.ts`
  - `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`
  - `src/games/plinko/renderer/plinko-bounce-playback-types.ts`
  - `src/games/plinko/renderer/matter-plinko-trajectory.ts`
  - `src/games/plinko/renderer/pixi-plinko-renderer.ts`
  - `src/games/plinko/renderer/plinko-playback-trajectory-resolver.ts`
  - `src/games/plinko/renderer/index.ts`
  - `src/games/plinko/lib/plinko-path.ts`
- Context-only files:
  - `src/games/plinko/lib/plinko-result.ts`
  - `src/games/plinko/lib/plinko-motion-plan.ts`
  - `src/games/plinko/model/use-plinko-manual-betting.ts`
  - `src/games/plinko/ui/plinko-game.tsx`
  - `.ai/context/plinko-mvp/curr-phase-4b.gif`
  - Plinko BFF and Provably Fair files.

## Source Of Truth And Decisions

- Inspected: `AGENTS.md`, `CLAUDE.md`, `docs/architecture/foundation-decisions.md`, relevant `.claude/rules/**`, `.claude/skills/implementation/SKILL.md`, the prior Plinko audit/plan, and current Plinko renderer/model files.
- The backend/BFF does not provide an independent bucket field. The renderer-local adapter derives `targetBucketIndex = sum(results)` from the accepted result; it does not represent an independent backend-authored bucket.
- Raw `results` remains unchanged for contract validation and Provably Fair diagnostics. Phase 1 does not wire the adapter into playback.

## Impact And Validation

- Documentation impact: not needed. Phase 4D and its visual-remediation correction replace only the existing Plinko-local renderer implementation behind its established interface; durable architecture documentation remains accurate because backend authority, renderer ownership, API boundaries, settlement semantics, and public contracts do not change. This rationale applies to `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`, `src/games/plinko/renderer/plinko-bounce-playback-types.ts`, `src/games/plinko/renderer/pixi-plinko-renderer.ts`, `src/games/plinko/renderer/plinko-playback-trajectory-resolver.ts`, `src/games/plinko/renderer/plinko-playback-profile.ts`, `src/games/plinko/renderer/plinko-renderer-types.ts`, and `src/games/plinko/renderer/index.ts`.
- API boundary impact: none. No browser request, BFF route, backend URL, auth, session, or DTO behavior changes.
- UI QA impact: Phase 4D visual QA must be rerun after the blocked GIF. Capture the GIF/video and the development `[Plinko playback]` source diagnostic together. Passing normal rounds must report `bounce-core`, show free-fall and visible contact-driven direction/speed changes, expose readable per-peg response, and reach the accepted bucket without rail-like stepping, corridor pull, snap, teleport, or magneting. Record candidate attempt/valid/selected diagnostics, fallback frequency/reasons, rows 8 and 14, Low/Medium/High, Turbo on/off, resize/board-change lifecycle, and only then AutoBet/Infinity smoke.
- Testing note: user explicitly declined test-infrastructure additions; this repository has no test command. Validation uses existing project commands only.
- Validation evidence:
  - `git diff --check` passed.
  - `pnpm lint` passed with one existing warning in `src/widgets/main-nav/main-nav.tsx` for unused `onExpandRequest`; no errors were reported.
  - Sandboxed `pnpm build` could not fetch the existing Google Outfit font. The user-approved escalated rerun compiled, type-checked, and completed successfully.
  - `pnpm check:docs` passed with this artifact's renderer-local docs-not-needed rationale.
  - Phase 2 `git diff --check`, `pnpm lint`, and `pnpm check:docs` passed. Lint retains the existing unrelated `main-nav.tsx` warning with no errors.
  - Phase 2 sandboxed build requires the existing Google Fonts network escalation; the user-approved escalated `pnpm build` compiled, type-checked, and completed successfully.
  - Phase 3 `git diff --check`, `pnpm lint`, and `pnpm check:docs` passed. Lint retains the existing unrelated `main-nav.tsx` warning with no errors.
  - Phase 3 sandboxed build requires the existing Google Fonts network escalation; the user-approved escalated `pnpm build` compiled, type-checked, and completed successfully.
  - Phase 4A `git diff --check`, `pnpm lint`, and `pnpm check:docs` passed. Lint retains the existing unrelated `main-nav.tsx` warning with no errors.
  - Phase 4A sandboxed build requires the existing Google Fonts network escalation; the user-approved escalated `pnpm build` compiled, type-checked, and completed successfully.
  - Phase 4B `git diff --check` passed.
  - Phase 4B `pnpm lint` passed with the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning; no errors were reported.
  - Phase 4B sandboxed `pnpm build` hit the existing Google Fonts `Outfit` fetch restriction from `src/app/layout.tsx`; the user-approved escalated rerun compiled, type-checked, and completed successfully.
  - Phase 4B `pnpm check:docs` passed with the updated Plinko renderer-local docs-not-needed rationale.
  - Phase 4B lifecycle follow-up `git diff --check` and `pnpm lint` passed; lint retains the existing unrelated `main-nav.tsx` warning with no errors.
  - Phase 4B lifecycle follow-up sandboxed `pnpm build` hit the existing Google Fonts `Outfit` fetch restriction from `src/app/layout.tsx`; the user-approved escalated rerun compiled, type-checked, and completed successfully.
  - Phase 4B lifecycle follow-up `pnpm check:docs` passed with the corrected task artifact rationale and wording.
  - Phase 4C `git diff --check` passed.
  - Phase 4C `pnpm lint` passed with the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning; no errors were reported.
  - Phase 4C sandboxed `pnpm build` hit the existing Google Fonts `Outfit` fetch restriction from `src/app/layout.tsx`; the user-approved escalated rerun compiled, type-checked, and completed successfully.
  - Phase 4C `pnpm check:docs` passed with the Phase 4C renderer-local docs-not-needed rationale.
  - Phase 4D `git diff --check` passed.
  - Phase 4D `pnpm lint` passed with the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning; no errors were reported.
  - Phase 4D sandboxed `pnpm build` hit the existing Google Fonts `Outfit` fetch restriction from `src/app/layout.tsx`; the user-approved escalated rerun compiled, type-checked, and completed successfully.
  - Phase 4D visual-remediation `git diff --check` passed.
  - Phase 4D visual-remediation `pnpm lint` passed with the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning; no errors were reported.
  - Phase 4D visual-remediation sandboxed `pnpm build` hit the existing Google Fonts `Outfit` fetch restriction from `src/app/layout.tsx`; the user-approved escalated rerun compiled, type-checked, generated all application routes, and completed successfully.
  - Phase 4D visual-remediation `pnpm check:docs` passed with the updated renderer-local docs-not-needed rationale.

## Risks And Handoff

- Risk: a future phase could accidentally pass raw `results` beyond this adapter. That boundary must be enforced when the playback engine is introduced.
- Risk: visual seed is deterministic from existing round and bet identity but has no gameplay or fairness meaning.
- Phase 1 source review: `createPlinkoVisualTarget()` is exported from the renderer-local index but has no production caller, so Pixi/Matter playback remains unchanged.
- Phase 2 handoff: `PlinkoPlaybackEngine` accepts only board geometry, `PlinkoVisualTarget`, and a semantic `PlinkoPlaybackProfile`. It does not accept raw backend `results`, has no legacy delegation, and is not wired into runtime playback.
- Remaining risks: Phase 3/4 must introduce target-only planning and Matter migration without expanding the engine contract to accept raw `results`; normal/Turbo semantic profiles need reference-driven calibration only after playback-migration approval.
- Phase 3 handoff: the corridor plan uses only target, geometry, profile, and deterministic seed. Its broad upper/middle/lower bands are not row checkpoints, and its fallback contains only a launch point, funnel, settle point, and target bucket.
- Remaining risks: Phase 4 must consume the corridor plan in a Matter implementation without adding visible steering or reintroducing raw `results`; launch/corridor/funnel ratios require reference-led visual QA before production use.
- Phase 4A handoff: the new Matter builder is unused by Pixi and receives no raw `results`. It accepts a sampled trajectory only after the simulated ball reaches the target bucket; otherwise it returns an invalid result and leaves later target-only fallback handling to a future cutover phase.
- Phase 4B handoff: `resolvePlinkoPlaybackTrajectory()` is the renderer-local target-only seam. It derives the visual-only target, selects the normal/Turbo profile from the already-available renderer option, builds the target-only playback, and returns a typed target-only or legacy-fallback resolution. The accepted round reaches `createPlinkoVisualTarget()` only to derive `targetBucketIndex = sum(results)`; raw row-by-row results do not enter the new planner, engine, or target-Matter contracts.
- Phase 4B lifecycle safety: Pixi tracks pending trajectory resolution with each round and playback token. Cancellation, resize, and board changes synchronously settle pending rounds as `cancelled` and invalidate their later completion; destroy still invalidates pending work without settlement.
- Phase 4B performance safety: target-only simulation is bounded by both profile-derived duration and a conservative 360-sample limit; it has no retry loop and performs no new per-frame planning allocations during Pixi playback.
- Remaining risks: target-only convergence and visual steering need manual QA before the legacy fallback can be reduced; target-only trajectories currently carry no peg-contact mapping, and fallback use is intentionally not exposed to product UI or telemetry.
- Phase 4C diagnosis: the failed GIF cannot prove whether legacy fallback was frequent because prior code did not expose trajectory source. Source inspection now reports `target-only`, `legacy-matter-fallback`, or `legacy-motion-plan-fallback` with typed target status in development console as `[Plinko playback]`. Source review indicates valid target-only playback was likely over-constrained by small launch variation, early target alignment, uniform corridor guidance, and a narrow horizontal-velocity cap.
- Phase 4C tuning: normal/permissive launch variation and upper/middle corridor width are wider; stage target alignment is now delayed; upper/middle target guidance is lower; lower-board guidance/funnel remains stronger; and permissive horizontal velocity allows broader peg-column movement. The target-only builder still accepts only a simulated landing in the accepted backend-derived target bucket, otherwise legacy fallback remains active.
- Remaining risks: wider target-only motion may invalidate more trajectories, in which case development diagnostics must distinguish fallback frequency from visual tuning. No visual contact/particle/trail work is included. Turbo remains limited to its existing semantic profile and needs later polish.
- Previous Phase 4C QA handoff is superseded by the Phase 4D Decision Lock below; the next implementation must replace the primary visible motion model before manual QA can pass.

## Phase 4D Decision Lock

- Audit decision: the Phase 4B/4C target/corridor-guided visible runtime is product-invalid and rejected as the primary Plinko animation core. It cannot meet the approved free-fall, contact-driven visual goal through further tuning.
- Root cause: primary motion is built from target-aligned corridor bands, fixed-step horizontal guidance, and velocity clamping. Its target-only builder emits no physical peg contacts, so Pixi cannot render real per-peg response.
- Replacement direction: implement a renderer-local bounce/contact-driven Matter candidate simulation as the next primary visible playback core. This is a full visible-core replacement, not another tuning pass, corridor widening pass, launch-spread pass, safer guided-trajectory iteration, small patch, or cautious preservation of the current model.
- Candidate-only first: generate bounded deterministic launch candidates from the visual seed, simulate gravity and actual peg collisions, record physical contacts, and select only a naturally target-reaching candidate. Do not enable lower-board capture, hidden steering, snap, teleport, magneting, corridor guidance, or row-by-row backend checkpoints in the first replacement implementation.
- Current model status: `plinko-corridor-plan.ts` and `matter-plinko-target-trajectory.ts` must leave primary visible runtime. They may be referenced only for cleanup, fallback isolation, or historical context; upper/middle corridor guidance must not survive as the new core.
- Legacy fallback policy: legacy Matter/path and motion-plan playback may remain only as temporary development safety during the rebuild. A legacy fallback during normal manual QA is a bounce-core failure, not a passing product result.
- Invalid target policy: if bounded candidate-only physics cannot produce valid target-reaching trajectories often enough, report the measured validity failure, explain candidate causes, present options, recommend one, and stop for explicit human approval. Do not silently add assist, steering, corridor forces, capture, snap, or magneting.
- Lower-board assist policy: no lower-board assist/capture zone is approved for the first replacement step. It may be considered only after measured candidate-only QA failure and explicit human approval; any later assist must be late, subtle, lower-board-only, and non-magnetic.
- Contact core requirements: produce renderer-local per-ball contact data with peg identity, row/index when available, impact strength, directional cue, timing, and optional bounded contact-driven micro-stall/perch marker. Pixi peg visual response is a core replacement requirement, not optional polish.
- Micro-stall policy: a short visual perch marker is allowed only after a measured low-energy, non-glancing peg contact. It must stay bounded, rare, per-ball, and unrelated to settlement authority; it must not be manufactured by rewriting the sampled trajectory timeline.
- Authority and scope: accepted backend result remains authoritative and may derive only the final target bucket. The replacement must not use backend row-by-row `results` as a visual path or change BFF/API, result contracts, payout, balance, history, fairness, AutoBet pacing, layout, shell, dependencies, board geometry, or current multi-ball interaction scope.
- Future-ready constraint: do not add ball-ball collision, but keep playback state per round/per ball rather than assuming one global ball.
- Expected Phase 4D implementation files: new `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`; optional new `src/games/plinko/renderer/plinko-bounce-playback-types.ts`; modify `plinko-playback-trajectory-resolver.ts`, `pixi-plinko-renderer.ts`, `plinko-playback-profile.ts`, `plinko-renderer-types.ts`, `index.ts`, and this artifact.
- First implementation step: replace resolver primary playback with the bounded bounce/contact candidate builder and wire its actual contact data to Pixi animation/peg response while preserving the existing cancellation lifecycle and fallback isolation.
- Future QA pass: pass only when the bounce core, not legacy fallback, visibly free-falls through pegs, contact changes direction/speed, pegs visibly respond, brief contact-driven perch can occur, upper/middle motion is not target-steered, and the accepted bucket is reached without snap, teleport, or visible magneting. Otherwise fail and do not proceed to Phase 5/6.
- Open decisions: no decision blocks the first candidate-only bounce-core implementation. A later lower-board assist policy is explicitly deferred to measured QA evidence and new human approval.
- Phase 4D failed QA evidence: `.ai/context/plinko-mvp/cur-bounce-4d-replaced.gif` failed because the visible path still appeared step-guided, the physical rebound was muted, and per-peg response was too restrained. The recording did not include the development source diagnostic, so it could not prove whether bounce-core or legacy fallback rendered it.
- Phase 4D remediation outcome: `matter-plinko-bounce-trajectory.ts` now evaluates every bounded, seeded candidate that naturally reaches the accepted target bucket, then selects the highest renderer-local physical-quality score rather than the first valid candidate. The score rewards meaningful measured contacts, direction changes, lateral variation, and rebound while penalizing sparse, overly straight, slow, and excessive-duration motion. It does not inspect raw row-by-row `results`, reconstruct a backend path, or apply target/corridor force.
- Phase 4D contact and playback outcome: bounce contacts record pre-step and outgoing velocity, normal-relative speed, direction change, glancing state, and impact-scaled renderer response. Physics uses lower air damping, profile-specific restitution/terminal-speed safeguards that scale only genuinely excessive total speed, and no ordinary per-axis rebound clamp. Pixi replays bounce samples linearly so collision-resolved velocity changes are not smoothstepped away; legacy sampled fallback keeps its existing easing.
- Phase 4D Pixi outcome: board pegs remain individual renderer-local views keyed by geometry row/index. Measured contact response now drives stronger bounded scale and layered pulse feedback at the actual contact time. A micro-stall marker is emitted only for a measured low-energy, non-glancing contact, extends that contact's bounded response, and does not inject positional hold samples or affect settlement.
- Phase 4D diagnostics and fallback: development-only `[Plinko playback]` logging now includes bounce candidate attempted/valid/selected index/quality alongside source and invalid reason. Legacy Matter/path and motion-plan playback remain explicit development safety in `pixi-plinko-renderer.ts`; a normal manual-QA fallback is still a failure, not product success.
- Phase 4D manual QA checklist: record a GIF/video and the matching development `[Plinko playback]` source diagnostic; confirm normal successful rounds use `source: "bounce-core"`; compare against `.ai/context/plinko-mvp/cur-bounce-4d-replaced.gif`; exercise rows 8 and 14, Low/Medium/High, and Turbo on/off; verify free-fall, multiple contact-driven speed/direction changes, readable peg response, and only rare physically supported perch markers; reject rail-like stepping, corridor pull, snap, teleport, magneting, wrong bucket, or any normal-QA legacy fallback; record candidate validity/fallback reasons; smoke resize/board change before AutoBet/Infinity.
- Phase 4D remaining risks: candidate-only physics may still lack a valid target-reaching candidate for some bucket/row/profile combinations, and revised rebound strength needs visual/performance confirmation at real board scale. Measure source/fallback frequency in manual QA; do not add lower capture, steering, or hidden assistance without the explicit approval required by this decision lock.

## Integrated Production Renderer Rebuild

- Plan-validation result: approved with the renderer-local geometry, physical-pocket, production-path, and settlement safeguards described below. This supersedes the older Phase 4D policy only where it retained legacy normal-runtime fallback or treated geometry/bucket physics as out of scope.
- Scope correction: `src/games/plinko/lib/plinko-path.ts` is editable for renderer geometry; physical bucket/divider bodies and aligned visible bucket rendering are in scope. Browser/API/BFF, accepted-result authority, payout, balance, fairness, history semantics, AutoBet request pacing, shell layout, dependencies, and test infrastructure remain out of scope.
- Geometry calibration: renderer geometry now exposes linked visual/collision tokens. At compact fourteen rows, the former representative ratio was ball-to-lane `0.316`, collision-radius-sum-to-lane `0.525`, and ball passage clearance `-0.79px`. The rebuild targets ball-to-lane `0.16-0.20`, collision-radius-sum-to-lane at most `0.42`, and positive passage clearance of at least `0.18 lane`. Ball and peg collision radii now scale from lane width rather than fixed large minimums; visual halo remains non-colliding and capped from lane width.
- Physical pocket settlement: Matter now owns static pocket dividers at visible bucket midpoints plus an aligned pocket floor. Candidate validity requires physical entry into the accepted target pocket, bounded in-pocket low-speed stability below the settle depth for seven samples, and a `100-150ms`-class simulated settle. Nearest-center lower-board assertion, steering, snap, target force, magneting, hidden capture, and lower-board assist remain prohibited.
- Production runtime policy: normal gameplay resolves only `production-bounce-core`. Rejected route-steered Matter/path and corridor playback no longer dispatch from Pixi. When no candidate physically settles in the accepted target pocket, Pixi preserves the existing external `fallback` settlement behavior and emits a development-only typed no-valid-production-animation diagnostic; it does not render legacy motion.
- Candidate/playback changes: the deterministic candidate bank uses a production-quality budget for Normal and Turbo. Turbo changes replay elapsed time only; it no longer reduces candidate count, restitution, contact response, or physical settle opportunity. Candidate scoring retains contact energy, direction change, lateral range/travel, rebounds, and anti-rail penalties; only physically settled target candidates are selectable.
- Matter/Pixi handoff: Pixi replays the sampled Matter position and velocity linearly at collision timing. It derives visual motion only from samples and recorded contacts; it does not alter trajectory positions or outcome.
- Ball material feedback: the renderer now derives directional squash/stretch, rebound expansion, capped glow/impact halo, and a velocity-driven rotating highlight from sampled velocity and real contact normals. These are visual-only and non-colliding.
- Peg/contact feedback: exact pegs receive impact-scaled pulse, scale, alpha, and spring return. Up to three nearby pegs receive bounded lower-strength response when impact energy warrants it. Pulses are capped by lane-relative geometry tokens for compact readability.
- Diagnostics: development `[Plinko playback]` output now records production source/status, rows, risk, Turbo state, target/physically settled bucket, candidate attempts/valid/selected quality, settle duration/sample count, and board geometry metrics including viewport, scale category, radii, halo caps, lane/row spacing, ratio, and passage clearance.
- Documentation impact: not needed. The changes remain inside the already documented Plinko-local renderer ownership and preserve backend authority, API/BFF, settlement contract, and browser boundary. This docs-not-needed rationale applies to `src/games/plinko/lib/plinko-path.ts` and the changed `src/games/plinko/renderer/**` files.
- Validation plan: run `git diff --check`, `pnpm lint`, `pnpm build`, and `pnpm check:docs`. Manual QA must capture correctly named GIF/video plus matching diagnostics for rows 8/14, Low/Medium/High, Turbo on/off, compact/desktop/expanded surfaces, center/left/right targets, no legacy source, physical pocket settle, resize/board-change cancellation, then AutoBet/Infinity smoke.
- Validation evidence: `git diff --check`, `pnpm lint`, and `pnpm check:docs` pass. Lint retains only the pre-existing unrelated unused `onExpandRequest` warning in `src/widgets/main-nav/main-nav.tsx`. The sandboxed build cannot fetch the existing Google `Outfit` font from `src/app/layout.tsx`; the approved escalated `pnpm build` compiled, type-checked, generated all 23 static pages, and passed. A local unauthenticated renderer smoke check loaded `/games/plinko` with one 628×500 Pixi canvas and no renderer errors; it cannot exercise an accepted real round, compact viewport, or production-contact diagnostics without authentication.
- Manual QA remaining: authenticate and capture the required real accepted rounds with their matching development `[Plinko playback]` logs. Fail the pass for any no-valid-production-animation result, non-production source, visual snap/steering, wrong-pocket settlement, or cancellation regression.
- Remaining risks: physical pocket geometry can reduce candidate validity, especially at edge buckets or compact sizes. A no-valid diagnostic is a blocker for QA, not permission to reintroduce assist or legacy playback.

## Production Playback Stabilization Pass

- Audit result: focused implementation approved. The renderer direction, physical pockets, sampled Matter/Pixi playback, and no-legacy normal runtime policy remain unchanged. The two P0 causes are internal resize cancellation consuming accepted rounds before visible start, and candidate paths escaping the drawable top/playfield envelope while still being eligible to settle later.
- Scope: update only `src/games/plinko/ui/plinko-pixi-stage.tsx`, `src/games/plinko/renderer/pixi-plinko-renderer.ts`, `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`, `src/games/plinko/renderer/plinko-bounce-playback-types.ts`, `src/games/plinko/renderer/plinko-playback-trajectory-resolver.ts`, renderer type/export files as required by the narrow renderer-local contract, and this artifact. The manual betting model, BFF/API contracts, balance/payout/history/fairness semantics, AutoBet request pacing, page shell, dependencies, and legacy implementation files remain context-only.
- Resize stabilization: the Pixi stage now coalesces ResizeObserver/fullscreen layout changes into one delayed stable resize instead of issuing immediate, animation-frame, and timer resizes. Renderer-local resize cancellation retains the accepted playback context, redraws fresh geometry, and retries once. A second resize cancellation emits the typed terminal `resize-retry-exhausted` outcome and preserves existing cancellation settlement semantics. Board change and teardown remain terminal and are not replayed.
- Drawable-envelope stabilization: Matter now derives a board-coordinate envelope and safe top-entry corridor from the same board geometry used by Pixi. Candidate samples update extrema and are rejected before scoring/selection when they leave the allowed top, side, or lower drawable tolerance. No artificial top rebound, steering, snap, magnet, capture, target force, or lower-board assist was added.
- Lifecycle diagnostics: every renderer-owned round receives a stable playback ID and development diagnostics for `accepted`, `queued`, `simulated`, `started`, `completed`, `no-valid`, `cancelled`, `start-failed`, `playback-failed`, or stage `init-failed`. Events carry local round ID, retry count, geometry revision, target/source/candidate/settle data through the resolver selection, cancellation/failure reason, and terminal state. Stage initialization failure now settles the accepted round explicitly rather than leaving it silent.
- Candidate diagnostics: candidate output now includes a typed rejection histogram for wrong-pocket, target-entered/no-stable-settle, no-pocket-settle, timeout/sample-cap, out-of-bounds, and simulation-error outcomes, plus no-valid state and selected-path `minX`, `maxX`, `minY`, `maxY`, and envelope-boundary evidence.
- Quality and authority: Normal and Turbo retain the same production Matter candidate budget and physical pocket validity. Turbo replay pacing remains unchanged. Peg collision, ball material response, peg/neighbor response, and production-only resolver dispatch remain intact. Backend accepted result remains authoritative; the renderer still only derives its visual target and never calls APIs or decides payout/outcome.
- Test-first constraint: this task introduces no test infrastructure because the approved artifact and user scope keep test infrastructure out of scope and `package.json` exposes no test command. Mechanical evidence uses the project-approved lint/build/docs/diff commands; authenticated renderer QA is required for accepted-round behavior.
- Documentation impact: not needed. This is a renderer-local stabilization of the already documented Plinko renderer/Matter playback ownership with no durable API, state, BFF, or product-contract change. The rationale applies to the stabilization files listed above.
- Validation and QA: final `git diff --check`, `pnpm lint`, escalated `pnpm build` when the sandboxed Google Outfit fetch is blocked, and `pnpm check:docs` remain required after the final source review. Authenticated QA must record grouped lifecycle diagnostics for normal rounds, resize/fullscreen changes, rows 8/14, Low/Medium/High, Turbo on/off, compact/desktop, and center/edge targets; it must reject silent outcomes, escaped selected paths, legacy runtime playback, duplicate settlement/history, or untyped no-valid results.
- Remaining risk: the new histogram must establish real candidate validity rates before any candidate-bank, launch-fan, or geometry tuning is considered. Compact/edge targets remain the highest-priority measurement profile; a no-valid result is diagnostic evidence, not permission for hidden assist.
- Validation evidence:
  - `git diff --check` passed.
  - `pnpm lint` passed with only the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning and no errors.
  - The sandboxed `pnpm build` stopped at the known Google Outfit fetch restriction in `src/app/layout.tsx`; the approved escalated rerun compiled, type-checked, and generated all 23 application pages successfully.
  - `pnpm check:docs` passed with this renderer-local docs-not-needed rationale mapped to the modified Plinko source files.
  - Local unauthenticated UI smoke: `/games/plinko` loaded with one Pixi canvas, risk selection redrew the board, compact sizing and desktop-to-compact resize retained one canvas, and no renderer console error was observed. The browser screenshot API timed out, and authentication was unavailable, so no accepted-round lifecycle, real candidate rejection, or animation recording was produced locally.

## Accepted-Round Playback-Start Reliability Fix

- Latest v2 evidence: `.ai/context/plinko-mvp/cur-prod-like-rerender-v2.gif` is now present beside the MP4. Its available visual evidence supports the focused diagnosis: production-like bounce playback and peg response look materially improved when a ball starts; the outstanding problem is accepted single manual bets that do not visibly enter playback while repeated attempts eventually do. This pass does not alter Matter physics, geometry ratios, candidate count, launch fan, visual material behavior, or legacy runtime routing.
- Gate 0 source confirmation: all six assumptions were confirmed before editing. `use-plinko-manual-betting.ts` computed `controlsLocked` from active/reconciling rounds but omitted it from both manual button eligibility and `placeManualBet()`; the Pixi stage recorded a round as dispatched before calling the void renderer command; `visualizeRound()` could silently return on a destroyed renderer or duplicate context; the 4.5s watchdog settled without correlated lifecycle state; and diagnostics began only after renderer ownership. The source-level root cause is therefore the accepted-round model/stage/renderer handoff, not bounce physics or board geometry.
- Focused scope: editable files for this pass are `src/games/plinko/model/use-plinko-manual-betting.ts`, `src/games/plinko/ui/plinko-controls.tsx`, `src/games/plinko/ui/plinko-game.tsx`, `src/games/plinko/ui/plinko-board-panel.tsx`, `src/games/plinko/ui/plinko-pixi-stage.tsx`, `src/games/plinko/renderer/pixi-plinko-renderer.ts`, `src/games/plinko/renderer/plinko-renderer-types.ts`, `src/games/plinko/renderer/plinko-bounce-playback-types.ts`, `src/games/plinko/renderer/plinko-playback-trajectory-resolver.ts`, `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`, `src/games/plinko/renderer/index.ts`, and this artifact. `plinko-game.tsx` and `plinko-board-panel.tsx` are a narrow required callback bridge from the model to the existing renderer options; `matter-plinko-bounce-trajectory.ts` has only a narrowed fallback-reason type to preserve the new typed lifecycle handoff, with no physics behavior change. No new state owner, API client, BFF route, dependency, or product feature was added. Geometry and candidate simulation behavior remain intentionally unchanged.
- Manual Bet lock: the manual UI now consumes the active-round lock, and `placeManualBet()` uses an immediate ref guard plus the existing unsettled/reconciliation guard. That makes request, queued, active, and unsettled terminal-reconciliation periods sequential even before React can render the disabled button. AutoBet still uses its existing `placePlinkoRound()` path and pacing.
- Renderer acknowledgement: `PlinkoRenderer.visualizeRound()` now returns a typed receipt: `accepted`, `already-active`, retryable `not-ready`, or terminal `rejected`. The Pixi stage retains a queued accepted round until it receives that receipt, records dispatch only after acknowledgement, retries renderer-not-ready dispatch at most twice, and emits a typed terminal enqueue failure rather than silently dropping a round. Renderer resolver/start failures remain explicit lifecycle terminals and retain existing external settlement reasons.
- Watchdog and lifecycle diagnostics: the model now records an accepted-response timestamp and per-round lifecycle state before renderer entry. Development-only diagnostics cover `model-request-started`, `model-response-accepted`, `model-round-created`, `model-visual-queue-added`, `stage-held-awaiting-renderer`, `stage-dispatch-attempted`, `stage-dispatch-acknowledged`, `stage-dispatch-rejected`, `stage-dispatch-skipped-duplicate`, `renderer-enqueue-accepted`, `renderer-enqueue-rejected`, `renderer-started`, `renderer-completed`, `renderer-terminal-no-valid`, `renderer-terminal-cancelled`, `renderer-terminal-start-failed`, `model-fallback-scheduled`, `model-fallback-fired`, and `model-visual-queue-cleared`. Records carry local round ID, backend bet ID once available, playback ID once allocated, target, rows, risk, Turbo state, accepted-response elapsed time, retry/renderer state, and terminal reason. The existing 4.5s watchdog now records whether renderer entry, visible start, or a renderer terminal occurred; it emits typed `model-watchdog-no-visible-playback` when a round never visibly started, and `model-watchdog-renderer-timeout` when a started renderer did not terminally finish in time.
- Authority and protected behavior: backend results remain the sole authority for accepted result, payout, balance, history, and fairness; the renderer still only replays an already accepted result and never calls APIs. No AutoBet request pacing, settlement reason, mini-history trigger, API/BFF boundary, or no-legacy production runtime policy changed.
- Documentation impact: not needed. These changes fix an existing Plinko-local model/stage/renderer lifecycle contract without creating a new state owner, game module, BFF/API boundary, or durable product behavior. `docs/architecture/foundation-decisions.md` remains accurate for `src/games/plinko/model/use-plinko-manual-betting.ts`, `src/games/plinko/ui/plinko-{controls,game,board-panel,pixi-stage}.tsx`, and the changed `src/games/plinko/renderer/**` files.
- Validation evidence so far: `git diff --check` passed. `pnpm lint` passed with only the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning. Sandboxed `pnpm build` reached the existing Google Outfit fetch restriction in `src/app/layout.tsx` before app compilation; the requested escalated retry could not be run because the environment rejected it for an account usage limit. `pnpm check:docs` passed after this artifact update. Browser-based local QA was blocked by browser security policy for `http://localhost:3000`; no alternate browser surface was used.
- Required authenticated manual QA: capture DevTools logs and visible board for (1) one single initial manual Bet through accepted response and started/terminal lifecycle, (2) repeated manual attempts while active proving the second submit is disabled/guarded and creates no second accepted round, (3) a Bet while renderer/layout stabilization is occurring proving held/retry or explicit terminal output, (4) resize/DevTools layout change during pending and active playback with no silent loss/duplicate settlement/history, and (5) rows 8/14, Low/Medium/High, Turbo on/off, compact/desktop, and center/edge targets where practical. Reject silent accepted outcomes, legacy runtime playback, or candidate/compact-profile regressions.
- Remaining risks: authenticated production-response QA remains unavailable locally, so candidate validity by target and compact board profile is unmeasured in this pass. The model watchdog is intentionally still a safety fallback; if it fires, its typed record is now diagnostic evidence for a lifecycle defect rather than permission to change physics or reintroduce legacy playback.

## Candidate Envelope / Out-of-Bounds Validity Fix

- Restored handoff update: authenticated QA confirmed accepted rounds reach the renderer pipeline through `renderer-simulated`; the single-Bet blocker is `renderer-terminal-no-valid` with `failureReason: "out-of-bounds"`, not model/stage enqueue or Manual Bet lock behavior.
- Gate 0 source confirmation: the candidate builder used one center-point rectangle for every sample. It did not distinguish spawn/top-entry, active playfield, or pocket/floor motion; it did not account for the collision body's radius; and its aggregate failure label returned `out-of-bounds` whenever even one rejected candidate escaped, regardless of the dominant rejection category. This made normal launch/pocket motion vulnerable to a false out-of-bounds classification and made the terminal label insufficient for diagnosis.
- Focused envelope change: candidate validation now uses explicit collision-body center bounds for three stages: controlled spawn-entry, active playfield after first entry, and pocket/floor settle. The visual halo remains excluded. The outer bounds now allow a collision body to remain partly visible within the board tolerance, while top/side escape after active entry and lower pocket/floor mismatch remain terminal rejection reasons. Spawn clamping remains a separate first-row corridor and launch fan/physics constants are unchanged.
- Diagnostics: resolver lifecycle selection now exposes the stage bounds, first candidate spawn position, first escape candidate/index/sample/point/stage/side, and best rejected candidate index/score/extrema. Existing candidate attempts, valid count, rejection histogram, target, rows, risk, Turbo state, and geometry revision remain available. Aggregate failure reason now reflects the dominant rejection count instead of any single out-of-bounds rejection.
- Lifecycle protection: renderer acknowledgement, stage dispatch, Manual Bet lock, watchdog diagnostics, production-only playback, physical peg collision, physical pocket settle, and no-assist/no-legacy policies are unchanged. `plinko-renderer-types.ts` receives only the already-emitted `resize-retry-exhausted` failure literal; `plinko-pixi-stage.tsx` receives only a browser timer ref type correction required for strict build validation.
- Test constraint: no test command exists in `package.json`, and the approved scope keeps new test infrastructure out of scope. Evidence for accepted-round behavior remains authenticated manual QA plus repository lint/build/docs/diff gates.
- Validation evidence: sandboxed `pnpm build` stopped at the existing Google Outfit font fetch restriction; the approved networked rerun then completed compilation, TypeScript, and all 23 pages after the two narrow pre-existing lifecycle type declarations were corrected. Final `git diff --check`, `pnpm lint`, and `pnpm check:docs` remain required after this artifact update.
- Required authenticated QA: start with five single Bets at rows 8 / Low / Turbo off / desktop / center target. Each must reach `renderer-started` then `renderer-completed`, with no `no-valid/out-of-bounds`. Record the lifecycle chain, candidate attempts/valid count, rejection histogram, stage bounds, spawn, first escape when present, target/settled bucket, and terminal reason. Then cover rows 8 and 14; Low/Medium/High; Turbo on/off; compact/desktop; and practical center/edge targets. Reject selected top/side escape, legacy source, steering/snap/magnet/capture, or duplicate settlement/history.
- Remaining risk: candidate validity is still unmeasured across real target buckets and compact profiles. If no-valid remains after this semantic fix, use the new histogram and first-escape evidence to distinguish true escape from wrong-pocket, no-settle, timeout, or simulation failure before adjusting any launch or candidate constants.

## Candidate Settle / Sample-Cap Reliability Diagnosis

- Latest authenticated QA: accepted manual rounds now reach renderer simulation and some complete through `renderer-started` and `renderer-completed`. The remaining baseline failure at rows 8 / Low / Turbo off / target bucket 4 is `renderer-terminal-no-valid` with dominant `timeoutOrSampleCap: 13`, `wrongPocket: 3`, `outOfBounds: 1`, and `candidateValid: 0`. This confirms the staged envelope change reduced the previous dominant out-of-bounds issue, but candidate validity remains unstable.
- Gate 0 source result: at 60 Hz, `MAX_SAMPLES` permits roughly 8 seconds, but rows 8 uses `getSimulationDuration()` = `8 * 235 + 1060` = 2940 ms. The observed timeout class therefore means the simulation duration expired, not the 480-sample cap. Current source did not retain enough per-candidate terminal state to prove whether these candidates were above the pocket, in/out of target pocket, in a wrong pocket, or merely above the settle-speed/depth condition at expiry.
- Decision: diagnostics-only pass. No duration, sample budget, settle threshold, pocket detection, candidate fan/coverage, scoring, restitution, floor/divider, geometry, or visual-motion constant was changed. Tuning any of those without the terminal state would be speculative and is intentionally deferred.
- Timeout diagnostics: every timed-out candidate now records candidate index, `duration` versus `sample-cap`, sample count and both caps, final position/velocity/speed, final/last pocket index, last visual bucket estimate, whether any pocket was entered, target-pocket entry/exit, above-pocket state, low-speed/outside-settle-depth state, settle duration/sample count, quality score, and trajectory extrema. Candidate diagnostics retain the first timeout candidate and the highest-quality timeout candidate; resolver lifecycle selection exposes both to existing development `[Plinko playback]` output together with previous envelope and rejection diagnostics.
- Scope protection: production-only Matter playback, physical pocket settling, staged collision-body envelope, lifecycle/acknowledgement/manual lock/resize behavior, no-legacy policy, and no steering/snap/magnet/capture/assist remain unchanged. No API/BFF, balance, payout, fairness, history, AutoBet, shell, or dependency change was made.
- Test constraint: the repository has no test command and approved scope keeps new test infrastructure out of scope. This pass uses source diagnosis plus lint/build/docs/diff validation; authenticated QA is required to establish the next minimal behavior fix.
- Validation evidence: sandboxed `pnpm build` stopped at the known Google Outfit fetch restriction; the approved networked rerun compiled, type-checked, and generated all 23 pages successfully. Final `git diff --check`, `pnpm lint`, and `pnpm check:docs` remain required after this artifact update.
- Required next authenticated QA: run five rows 8 / Low / Turbo off / desktop / center-target single Bets. For every timeout, capture the full lifecycle plus `candidateFirstTimeoutCandidate` and `candidateBestTimeoutCandidate`. Determine whether the dominant pattern is (a) above-pocket at the 2940 ms duration cap, (b) target entered then exited, (c) target entered but speed/depth never satisfied, (d) wrong-pocket coverage, or (e) an actual 480-sample cap. Only then approve one minimal duration, settle, pocket, or candidate-coverage change.

## Target-Aware Candidate Coverage Fix

- Audit conclusion: the previous deterministic 17-candidate fan was symmetric and generic. It used the target only as part of the visual seed, then filtered for strict physical settlement afterwards; it could exhaust all candidates without entering the accepted target pocket. Source review reconfirmed the backend-derived target index and renderer physical-pockets share the same `0..rowsCount` indexing, so no contract/indexing change is needed.
- Implementation decision: the production Matter builder now creates two deterministic, bounded candidate batches for the one accepted backend target only. Batch 1 contains 8 target-aware candidates. Its launch offset and horizontal velocity vary around a target-relative bias derived from the target bucket centre, board start point, lane spacing, and row count. Batch 2 contains at most 9 broader fallback candidates, retaining diversity while still blending the same target-relative bias. The target-aware batch stops on its first strict physical target-pocket settle; Batch 2 runs only when Batch 1 has no valid result and also stops on its first valid result.
- Candidate budget: before = 17 generic candidates; after = 8 target-aware + at most 9 fallback = 17 maximum. A successful Batch 1 executes fewer than the previous hard ceiling. There is no `bucketCount × candidateCount` loop, all-bucket simulation, cache, or precomputation; every simulation is evaluated solely against the already accepted `targetBucket`.
- Validity and physics: validity remains exact physical settlement in the required pocket. Near-target, wrong-pocket, timed-out, and out-of-bounds candidates remain rejected. Duration cap, 480-sample cap, Matter gravity, collision response, geometry, scoring, and renderer settlement authority are unchanged. No steering, snap, magnet, capture, target force, or lower-board assist was added.
- Diagnostics: existing timeout/envelope/rejection diagnostics remain intact. Candidate diagnostics and resolver selection now expose target-aware/fallback attempt counts, Batch 1 validity, fallback use, per-batch and total simulation milliseconds, selected batch and zero-based batch index, selected global index, valid/rejection counts, and the closest rejected candidate's final visual bucket plus target distance in lane units. Existing selection already exposes the authoritative target bucket and selected physically settled bucket to the lifecycle event.
- Files changed: `src/games/plinko/renderer/matter-plinko-bounce-trajectory.ts`, `src/games/plinko/renderer/plinko-bounce-playback-types.ts`, `src/games/plinko/renderer/plinko-playback-trajectory-resolver.ts`, and this active artifact.
- Documentation impact: durable documentation is not needed. This is a local correction inside the already documented Plinko renderer/Matter replay ownership; it creates no API/BFF, state, module, validation, or product-contract pattern. `docs/architecture/foundation-decisions.md` remains accurate for the changed `src/games/plinko/renderer/**` source area.
- Validation evidence: `git diff --check` passed. `pnpm lint` passed with the existing unrelated `src/widgets/main-nav/main-nav.tsx` unused `onExpandRequest` warning and no errors. The sandboxed `pnpm build` was blocked before compilation by the existing Google Outfit request in `src/app/layout.tsx`; the approved network-enabled rerun compiled, TypeScript-checked, and generated all 23 routes successfully. `pnpm check:docs` passed with this artifact's renderer-local docs-not-needed rationale. Final semantic review found no scope, renderer-authority, API-boundary, or docs-truthfulness issue.
- Required authenticated QA: run five single desktop bets at rows 8 / Low / Turbo off, recording target bucket, terminal phase, total/batch attempt counts, fallback use, total/batch simulation milliseconds, valid/rejection counts, selected batch/index, target bucket, and physically settled bucket. Expected normal lifecycle is `renderer-simulated` → `renderer-started` → `renderer-completed`, without a common no-valid trace where target-pocket entry is always false. Then run a small repeated-click smoke with the existing Manual Bet behavior unchanged; inspect diagnostics for synchronous timing spikes, playback starvation, duplicate settlement/history, and any accidental all-bucket behavior. Only after baseline improvement, extend through rows 8/14, risks, Turbo, compact/desktop, and practical centre/edge targets.
- Remaining risk: authenticated production-response QA remains unavailable in this workspace, so target coverage, UI responsiveness, compact geometry, edge buckets, and production burst behavior remain unmeasured. UI QA is therefore blocked for an accepted round, not waived. The newly recorded timing is evidence to assess those risks, not a claim that burst safety has been proven.
